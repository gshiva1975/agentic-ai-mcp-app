FROM python:3.12-slim

WORKDIR /app

COPY . .

RUN pip install --no-cache-dir fastapi uvicorn requests

EXPOSE 8003

CMD ["uvicorn", "mcp_servers.market_server:app", "--host", "0.0.0.0", "--port", "8003"]
