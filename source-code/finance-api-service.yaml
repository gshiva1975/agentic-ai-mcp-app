apiVersion: v1
kind: Service
metadata:
  name: finance-api-service
spec:
  selector:
    app: finance-api
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30080       # access via: minikube service finance-api-service
  type: NodePort
